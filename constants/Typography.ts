/** Loaded in root `_layout` via `useFonts`; use these names in `Text` styles. */
export const Fonts = {
  body: 'Inter_400Regular',
  medium: 'Inter_500Medium',
  semi: 'Inter_600SemiBold',
  bold: 'Inter_700Bold',
} as const;
